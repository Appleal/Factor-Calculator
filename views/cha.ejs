<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <title>Document</title>
    <style type="text/css">
        .wrap{
            background:#eee;
            width: 900px;
            min-height: 400px;
            border-radius:10px;
            border:1px solid #eee;
            padding:20px;
            margin:20px auto;
        }
        #numTxt{
            width: 60%;
            font-size:22px;
            border-radius:10px;
            border:1px solid #eee;
            padding:0 5px;
        }
    </style>
</head>
<body>
<div class="wrap">
    <h1>因数计算器 - 显示结果</h1>
    <div class="box">
        <p>你查询的数字是：<%= num %>，它的因数有<%= result.length %>个，分别是：</p>
        <ul>
            <%
            for(var i = 0;i < result.length;i++){
            %>
            <li><%= result[i] %></li>
            <%
            }
            %>
        </ul>
    </div>
</div>
</body>
</html>